auto-install-peers=true
strict-peer-dependencies=false
# Some global ~/.npmrc set `optional=false`; Vite/esbuild ship their
# platform binaries as OPTIONAL deps, so we must keep optional installs on.
optional=true
